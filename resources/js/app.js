// Preline v4 restructured its package exports: the old
// 'preline/dist/preline.js' path is no longer exported. The '.' entry
// (./dist/index.mjs) is the auto-init build — same behaviour as before;
// 'preline/non-auto' is the manual-init variant.
import 'preline';